# Battle HUD Parity & Completeness Audit

> **Provenance.** Generated by the `battle-hud-parity-audit` multi-agent workflow (2026-07-03):
> 9 catalog agents (6 sighted-HUD regions + 3 mod slices) → 2 cross-ref agents (completeness + parity)
> → 34 adversarial per-finding verifiers (default-to-refuted) → 1 synthesis. 44/46 agents succeeded;
> 26 findings confirmed, 6 refuted. The 2 verifiers that died on a StructuredOutput retry cap were
> re-verified by hand afterward — see **Appendix: recovered verifications** at the end.

## Executive summary

The mod is **broadly at parity** for turn-based combat: the large majority of the ~180 sighted-HUD information items across turn-order, resources/vitals, action-bar, overtips, reticle-prediction, and combat-log/floating-text surfaces already reach a blind player (full combat-log corpus via `LogTap`, core hit prediction via `HitPredictor`, passive cover/range/threat via `CombatReads`, AP/MP + momentum/veil/PF gauges, buff gain/loss + full buff lists, the complete action-bar slot readout, weapon-set swaps, AoE shape/facing/legality, and the deployment flow). **No systemic parity failure exists** — fog-of-war gating is generally sound because `Entity.IsVisibleForPlayer` already folds in `!IsInFogOfWar`. There are **24 verified completeness gaps** (most medium/low, clustering in three areas: the movement-preview economy, kill/consequence reticle flags, and the name-only initiative turn-order review list) and **2 verified parity leaks** (one high-severity: an ungated buffer read of enemy HP/defenses/buffs; one medium: exact HP leaking past the game's `HideRealHealthInUI` "???" mask) — **both now fixed (2026-07-03, compile-clean)**. The single un-mirrored masking rule across the whole mod was `HideRealHealthInUI`; it is now honored at every direct HP read site.

---

## (1) Completeness gaps (Rule 1) — ranked most decision-critical first

### Medium severity

#### 1. Ability "ends turn" / "spends all movement points" cue is missing from the slot focus readout
Missing the cue risks an unintended full-turn loss in turn-based combat.
- **Sighted reads it at:** `SurfaceActionBarVM.cs:276-282` (`EndTurnText` on hover) / `GetEndTurn` `:327-335` (returns `Tooltips.EndsTurn` / `SpendAllMovementPoints` when the blueprint has `WarhammerEndTurn`).
- **Mod status:** *partial* — surfaced only in the Space full-tooltip scrape (`TooltipTemplateAbility.cs:432-439`, brick render `:827-831`); absent from `RTAccess/UI/Proxies/ProxyActionBarSlot.cs:82-115` `State()` which reads AP/range/target/uses/ammo/cooldown but never `EndTurnText`/`WarhammerEndTurn`.
- **Fix:** In `ProxyActionBarSlot.State()`, when `ab != null`, replicate `GetEndTurn`: read `ab.Blueprint.GetComponent<WarhammerEndTurn>()` (honor `CheckBuffForMPSpendTooltip`) and append the game-localized `UIStrings.Instance.Tooltips.EndsTurn` / `SpendAllMovementPoints` (pass-through) so the flag fires the moment the slot is focused.

#### 2. "Possible to kill" death-mark (lethal-shot skull) not surfaced
A decisive at-a-glance targeting cue, and the data is already in hand.
- **Sighted reads it at:** `OvertipHitChanceBlockVM.cs:52,144` (`CanDie = MaxDamage >= HitPointsLeft + TemporaryHitPoints`, gated `IsVisibleForPlayer` `:80`) and `OvertipHealthBlockVM.cs:26,146` (suppressed under `HideRealHealthInUI` `:111`).
- **Mod status:** *missing* — `RTAccess/Accessibility/HitPredictor.cs:100-133` `Compose` emits hit%/crit/cover and (verbose only) damage but no lethal comparison; whole-mod grep for `CanDie|PossibleToKill|CanKill` = 0.
- **Fix:** In `HitPredictor.Describe` compute `canDie = ui.MaxDamage > 0 && ui.MaxDamage >= target.Health.HitPointsLeft + target.Health.TemporaryHitPoints`, pass into `Compose`, and append a new localized `predict.can_kill` clause in **both** terse and verbose output. Suppress when `target.HasMechanicFeature(MechanicsFeatureType.HideRealHealthInUI)` to mirror the overtip.

#### 3. Overpenetration chain preview (reduced hit/damage on each pierced in-line target) — plus a correctness bug
- **Sighted reads it at:** `AbilitySingleTargetRange.cs:92-145` builds `m_AbilityTargets` threading one `ref OverpenetrationUIData` so each in-line target's hit/damage falls off (`AbilityTargetUIData.cs:143-167`); `OvertipHitChanceBlockVM.cs:99-106` floats a reduced overtip over every pierced unit.
- **Mod status:** *missing* — `HitPredictor.cs:59` reads exactly one target via `cache.GetOrCreate`; grep for `overpenetrat|GetSingleShotAffectedNodes|GatherAffectedTargets` = 0. **Compounding bug:** `AbilityTargetUIDataCache.cs:78-96` `GetOrCreate` on a miss builds virgin `OverpenetrationUIData` (100/100), so cycling the scanner onto a secondary target yields **unreduced** numbers.
- **Fix:** When aiming a single-shot ability, enumerate the line via `ability.GetSingleShotAffectedNodes(target)` then `ability.GatherAffectedTargetsData` to get each pierced target's reduced `AbilityTargetUIData`; append a chain tail naming each pierced unit with its reduced hit/damage. Gate each secondary on `IsVisibleForPlayer` && `CurrentlySeen` (mirrors `OvertipHitChanceBlockVM.cs:80`). This also fixes the cache-miss correctness bug.

#### 4. AoE caught units — per-unit hit chance / damage / kill for each unit in the template
- **Sighted reads it at:** `OvertipHitChanceBlockVM.cs:99-106` (`HandleCellAbility` sets each caught unit's `AbilityTargetUIData` = hit%/damage/burst), fed by `AbilityPatternRange.cs:133-162` (`GatherAffectedTargetsData`).
- **Mod status:** *missing per-unit* — `RTAccess/Exploration/AoEPreview.cs:95-114` emits only aggregate enemy/ally counts + friendly-fire names; the single-target predictor (`Targeting.cs:62`) returns null unless `ability.CanTargetEnemies` and computes odds for *centering* on a unit, not collateral odds at the current aim center.
- **Fix:** In `AoEPreview.CursorTail`, after computing `pattern`, call `ability.GatherAffectedTargetsData(...)` (same API `LineOfSightVM`/`AbilityPatternRange` use). For each caught unit passing the existing `IsVisibleForPlayer`/awake/in-combat gate, append a terse name + rounded `HitWithAvoidanceChance`% (+ kill flag when `MaxDamage ≥ HP`); full damage range on verbose Space.

#### 5. Ricochet-target uncertainty ("???" hit chance)
- **Sighted reads it at:** `OvertipHitChanceBlockView.cs:113-124` sets hit text to `"???"` when `UnitPredictionManager.IsUnitRicochetTarget(unit)`, `:106-108` force-shows the ricochet overtip with 0 damage; built by `AbilitySingleTargetRange.cs:116-127` (`SetRicochetPreview`).
- **Mod status:** *missing* — `HitPredictor.cs:38-69` handles a single `(caster, ability, target)` tuple; sole caller `Targeting.cs:56-66` passes one `item.TargetUnit`; grep `Ricochet` = 0 source hits.
- **Fix:** In `HitPredictor`/`Targeting`, when aiming a ranged attack, detect `UnitPredictionManager.Instance.IsUnitRicochetTarget(unit)` for scanned units (or enumerate the `AbilityTargetUIData` entries the game builds), announce them as caught ricochet targets and speak the uncertain state (localized `predict.ricochet_unknown`) instead of a percentage, mirroring the game's "???"/0-damage overtip.

#### 6. Buff DOT damage-per-tick number on the buff icon
- **Sighted reads it at:** `BuffPCView.cs:51-59` (for `IsDamage` buffs hides rank, shows `m_Damage` = DOT AverageValue); `BuffVM.cs:186-193` `CalculateDamage` = `DOTLogicUIExtensions.CalculateDOTDamage(Buff).AverageValue`.
- **Mod status:** *missing* — `RTAccess/Buffers/UnitBuffer.cs:82-89` `BuffLine` reads only `buff.Rank` + duration; grep `DOTLogic|CalculateDOTDamage|AverageValue` = 0.
- **Fix:** In `UnitBuffer.BuffLine`, detect DOT via `buff.Blueprint.IsDOTVisual` (or `GetComponent<DOTLogicVisual>()`); replace the rank annotation with `DOTLogicUIExtensions.CalculateDOTDamage(buff).AverageValue` under a new `buffer.dot_damage` key. Keep rank display for non-DOT buffs.

#### 7. Per-card status markers: will-lose-turn / control-loss / unable-to-act
- **Sighted reads it at:** `SurfaceCombatUnitVM.cs:44-48` (`IsUnableToAct`/`WillNotTakeTurn`/`HasControlLossEffects`), computed `:307-332` from `!CanActMechanically`, `!WillTakeTurn` (folds Stunned/IsHelpless/IsProne), `HasControlLossEffects()`; 3-icon priority cascade at `SurfaceCombatUnitView.cs:162-167`.
- **Mod status:** *missing* — grep `WillNotTakeTurn|HasControlLossEffects|IsUnableToAct|CanActMechanically|Stunned|IsHelpless|IsProne` = 0; locale key `hud.initiative` (`ui.json:144`) is orphaned. Only indirect coverage is named non-hidden buffs (`UnitBuffer.cs:82-89`) and the after-the-fact "missed turn" log line (`LogTap.cs:84`).
- **Fix:** Add a shared visibility-gated read mirroring the game's logic — Stunned || `State.IsHelpless` || `State.IsProne` → "will lose turn", else `HasControlLossEffects()` → "control loss", else `!State.CanActMechanically` → "unable to act" — appended to the combat suffix in `ProxyUnit.CombatSuffix` (allies and enemies), and/or wire the orphaned `hud.initiative` key into a real turn-order review. Gate every read on `IsVisibleForPlayer`/`FogProbe`.

#### 8. Faction (enemy/companion/friend) tag on the initiative review list
- **Sighted reads it at:** `SurfaceCombatUnitView.cs:181-193` sets `m_FractionViewMode` from `IsEnemy.Value`/`IsPlayer.Value`; fields ready on `SurfaceCombatUnitVM.cs:36-42,273-275`.
- **Mod status:** *partial* — `RTAccess/Screens/InGameScreen.cs:500-510` `InitiativeLabel` = name + optional "current" only; grep `enemy/faction/ally/hostile` in file = 0. The only faction cue (`combat.turn_enemy` `:444-448`) fires live at turn-start, not while scanning the queue.
- **Fix:** In `InitiativeLabel` append a localized allegiance tag: `vm.IsEnemy.Value` → `combat.faction_enemy`, else `vm.IsPlayer.Value` → `combat.faction_ally`, else `combat.faction_neutral`. Parity-safe (the tracker lists only shown participants — no fog gating needed).

#### 9. Per-card HP on the initiative review list
- **Sighted reads it at:** `SurfaceCombatUnitView.cs:37-42,130-134` binds HP text per card = `UIUtility.GetHpText` (`UIUtility.cs:1166-1174`), masked to "???" for `HideRealHealthInUI` (`CharInfoHitPointsVM.cs:73-79`).
- **Mod status:** *partial* — `InitiativeLabel` is name-only; HP for the same units is reachable only on a different surface (`ProxyUnit.cs:99-100` scanner + Inspect), not co-located on the queue.
- **Fix:** Append the unit's HP after the name. **Critical:** gate on `vm.Unit.HasMechanicFeature(MechanicsFeatureType.HideRealHealthInUI)` and speak a localized "???" token instead of exact numbers when set — do **not** reuse `AppendWounds` (`InGameScreen.cs:521-527`) or `ProxyUnit.Detail`, which read raw HP and would leak (see Leak #2).

#### 10. Next-round divider / round split in the turn-order queue (and surprise "S" marker)
- **Sighted reads it at:** `InitiativeTrackerVM.cs:169-177` (sets `RoundIndex`, appends next-turn preview into the same `Units` list); `InitiativeTrackerVerticalView.cs:113-117,197-208` injects a divider card; `InitiativeTrackerEndOfRound.cs:18-24` renders the round number or literal "S" for the surprise round.
- **Mod status:** *partial* — `InGameScreen.cs:477-480` walks `tracker.Units` flatly; grep `RoundIndex|RoundVM` = 0. Round number is conveyed transiently by `CombatEvents.cs:142-148` but gated `round>0` (`:145`), so the surprise round is never announced.
- **Fix:** In `RebuildCombat` read `tracker.RoundIndex` and insert a divider `TextElement` where `OrderIndex` crosses `RoundIndex`, labeled via `combat.next_round` from `tracker.RoundVM.Round.Value` (or `combat.surprise_round`). Optionally relax `CombatEvents.cs:145` to announce the surprise round.

#### 11. Awareness log line — trap / door spotted (voiced live)
- **Sighted reads it at:** `AwarenessLogThread.HandleEvent(GameLogEventAwareness)` emits only `TrapSpotted`/`DoorSpotted` — a discrete reveal event, not a frequent stream.
- **Mod status:** *partial* — `RTAccess/Accessibility/LogTap.cs:53` places `"AwarenessLogThread"` in the Noise set (captured for L review, never spoken live) with an incorrect "frequent" rationale. Only other coverage is discovery-via-scan (`ProxyMapObject.cs:151-152`).
- **Fix:** Remove `"AwarenessLogThread"` from `LogTap.Noise` (`LogTap.cs:49-54`) so `TrapSpotted`/`DoorSpotted` voice live. It is low-frequency and engine-gated on the awareness check, so it is neither chatty nor a leak.

#### 12. Veil predicted (after-cast) value when aiming a warp ability
- **Sighted reads it at:** `VeilThicknessVM.cs:21,73-83` computes `PredictedValue = current + GetVeilThicknessPointsToAdd(isPrediction:true)`; `VeilThicknessView.cs:27,50,66-69` renders it against the critical marker; `AbilityData.cs:1225-1242` shows caster `ChangeVeilDamage` facts can exceed the static value.
- **Mod status:** *partial* — the **static** per-cast cost is already on Space (`TooltipTemplateAbility.cs:414-420` `PsychicPowerCostAP` "N AP, +M veil" + degradation brick). Genuinely absent: the live predicted **absolute** after-cast value, the caster-fact-adjusted delta, and whether the cast crosses critical. Grep `PredictedValue|GetVeilThicknessPointsToAdd` = 0.
- **Fix:** In `ProxyActionBarSlot.AppendAbilityDetails` (or a targeting announcement), when `ab.Blueprint.IsPsykerAbility`, read `VeilThicknessVM.PredictedValue` (or compute `current + ab.GetVeilThicknessPointsToAdd(isPrediction:true)`) and flag if `≥ PsychicPhenomenaRoot.CriticalVeilOnAllLocation` (`slot.veil_after` / `slot.veil_would_go_critical`). Also add the critical threshold to `gauge.veil` (`HudGauges.cs:69`). No leak — veil is a global location value.

### Low severity

#### 13. Reachable / movable-area extent (the blue move-area highlight)
- **Sighted reads it at:** `UnitMovableAreaController.cs:44,228-244` (`CurrentUnitMovableArea` = `FindAllReachableTiles_Blocking(...).Keys`; `.Count` = extent).
- **Mod status:** *partial* — mod usage is per-tile only (`PathInfo.cs:46-72` single-tile MP cost; `InteractableDescriber.cs:130-143` per-tile reachable/cover). No aggregate readout; `HudGauges.cs` speaks no movement points.
- **Fix:** Add a one-shot aggregate readout during a controllable turn: `area.Count` reachable tiles (`Game.Instance.UnitMovableAreaController.CurrentUnitMovableArea`), optionally farthest distance/bearing + MP budget (`ActionPointsBlue`/`ActionPointsBlueMax`). Wire into `HudGauges` or the tile-explorer summary, gated `IsInCombat && controller.CurrentUnit != null`. New `path.movearea.summary`.

#### 14. Effective (half) range ring
- **Sighted reads it at:** `CombatHudAreas.cs:14` (`AbilityEffectiveRange` = own area flag) fed by `AbilitySingleTargetRange.cs:68` (`effectiveRange = Weapon != null ? FloorToInt(MaxRangeCells/2f) : 0`).
- **Mod status:** *missing* — `Targeting.cs:100-102` and `ProxyActionBarSlot.cs:130-133` speak max/min range only; grep `EffectiveRange|half.?range` = 0.
- **Fix:** In `Targeting.ArmAnnounce` and `ProxyActionBarSlot.AppendAbilityDetails`, when `ab.Weapon != null` and not melee, compute `eff = FloorToInt(r/2f)` and, if `0 < eff < r`, append `aim.effective_range` / `slot.effective_range`. Optionally flag "beyond effective range" in `HitPredictor.Describe`.

#### 15. Enemy difficulty rating (roman-numeral threat tier)
- **Sighted reads it at:** `SurfaceCombatUnitVM.cs:64,179-181` (`ShowDifficulty`), `SurfaceCombatUnitView.cs:196-203` (`ArabicToRoman(Difficulty)`); source `UIUtilityUnit.cs:196-203` (`= DifficultyType+1`).
- **Mod status:** *missing* — no combat `Difficulty` reference; `ProxyUnit.cs:122` reads only cover/range/threat.
- **Fix:** In `ProxyUnit`'s enemy detail (or initiative suffix), append the tier when `UIUtilityUnit.GetSurfaceEnemyDifficulty(unit) > 0` as a localized "threat tier N" (spell out the numeral for TTS). The existing visibility filter keeps it fog-safe; dropping the Hunter-only gate is reasonable parity+.

#### 16. Shield-equipped indicator on the target-defenses block
- **Sighted reads it at:** `OvertipTargetDefensesView.cs:68-73,112,135-139` (dedicated `m_ShieldBlock` driven solely by `ShieldEquipped`, independent of block%); raised on equip and on plain enemy hover (`OvertipHitChanceBlockVM.cs:66,237-274`).
- **Mod status:** *missing* — `HitPredictor.cs:119` appends `predict.blocked` only when block% > 0 and only inside `Describe()`; grep shield = 0. Sighted player gets the cue when block% rounds to 0 and on passive hover; blind player does not.
- **Fix:** In `HitPredictor.Compose`, when `ui.BlockChance` rounds to 0 but the target has a shield, still append `predict.shielded`. Detect via `target.GetBodyOptional()?.Hands.Any(h => h.HasShield)` (mirrors `CheckEntityHasShieldEquipped.cs`). Better: also surface it in the fog-gated Scanner/Inspect readout.

#### 17. Squad grouping and squad member count
- **Sighted reads it at:** `SurfaceCombatUnitVM.cs:62,281` (`SquadCount` = alive squad members); `SurfaceCombatUnitOrderView.cs:192-198` (squad number shown when `IsSquadLeader`).
- **Mod status:** *missing grouping* — grep `squad` = 0; underlying members are individually voiced by the scanner, so lower impact than raw "missing".
- **Fix:** In the scanner unit label / `BattlefieldSummary`, when `IsSquadLeader && HasAliveUnitsInSquad` append localized "squad leader, N alive" (`SquadCount.Value`); tag non-leaders "squad member". Source from `PartUnitSquad`/`m_SquadOptional`, gated on already-visible/`CurrentlySeen`.

#### 18. Weapon-ability ammo cost per activation (ammo-cost badge)
- **Sighted reads it at:** `ActionBarSlotVM.cs:50,222` (`AmmoCost`, distinct from Current/Max ammo); `SurfaceActionBarSlotWeaponAbilityView.cs:52-88` (separate `m_AmmoCost` badge, shown when > 0).
- **Mod status:** *missing from browse-label* — `ProxyActionBarSlot.cs:94-95` speaks only `slot.ammo` from CurrentAmmo/MaxAmmo; grep `AmmoCost` = 0 (reachable only via Space tooltip).
- **Fix:** In `ProxyActionBarSlot.State()`, read `_slot.AmmoCost.Value` and when > 0 append `slot.ammo_cost` ("{cost} ammo per use") alongside the existing ammo/AP readout.

#### 19. Ordinal position number in the queue ("unit order N")
- **Sighted reads it at:** `InitiativeTrackerUnitVM.cs:10,20` (`OrderIndex`); `SurfaceCombatUnitOrderVerticalPCView.cs:26-29` (hover hint `TurnBasedTexts.UnitOrder`).
- **Mod status:** *missing* — `InitiativeLabel` adds only "current" for the active unit; grep `OrderIndex|UnitOrder` = 0.
- **Fix:** In `InitiativeLabel` append `vm.OrderIndex?.Value` when > 0 via a localized `combat.unit_order` (mirrors `TurnBasedTexts.UnitOrder`), keeping "current" for index 0.

#### 20. Buff non-stacking-conflict warning marker
- **Sighted reads it at:** `BuffVM.cs:27,72-79` (`ShowNonStackNotification`), `BuffPCView.cs:47-49` (badge), `TooltipTemplateBuff.cs:205-210` (`TooltipBrickNonStack` lists overriding sources).
- **Mod status:** *missing* — `UnitBuffer.cs:82-89` reads name+rank+duration only; grep `NonStack` = 0. Parity-safe (`UnitPartNonStackBonuses.cs:82` is party-only).
- **Fix:** In `UnitBuffer.BuffLine`, query `buff.Owner?.GetOptional<UnitPartNonStackBonuses>()?.ShouldShowWarning(buff)` and append `buffer.non_stack` ("bonus overridden"); list overriding source names on Space via `GetBuffList`/`GetItemsList`.

#### 21. Momentum / Resolve running delta (per kill/turn) voiced live
- **Sighted reads it at:** `RulePerformMomentumChangeLogThread.cs:13-38` (every change emits `MomentumValueChanged` with delta + current + reason).
- **Mod status:** *partial* — `LogTap.cs:44` lists it in `OwnedElsewhere` (captured, reviewable, not live). Live momentum speech is only the heroic/desperate readiness one-shots (`CombatEvents.cs:196-203`) + the K gauge (`HudGauges.cs:49-58`).
- **Fix:** Add an opt-in (default-off) live cue for the two highest-value reasons only (KillEnemy, StartTurn), reusing the thread's localized reason strings; cheapest wiring is extending the readiness poll at `CombatEvents.cs:196-203`. Optionally append the current momentum value to the readiness cue.

#### 22. Veil-thickness running value/state-change log lines (voiced live)
- **Sighted reads it at:** `VeilThicknessLogThread.cs:9-18` (logs `VeilThicknessValueChanged` on every change).
- **Mod status:** *partial* — `LogTap.cs:52` places it in Noise (`:77` early-returns). Only live cue is the one-shot critical crossing (`CombatEvents.cs:205-214`); exact value only on demand via K (`HudGauges.cs:62-70`).
- **Fix:** Keep per-tick muted; edge-detect veil crossing coarse bands (quartiles of `MaximumVeilOnAllLocation`) in `CombatEvents.PollThresholds` and enqueue "veil rising/falling to N", and/or announce net delta at end of round. Optionally a settings toggle to un-mute the thread.

#### 23. AoE primary (application) cell vs splash cells distinction
- **Sighted reads it at:** `CombatHUDRenderer.cs:732-751` shades `m_PrimaryAoeArea` (application/`MainCell` cells) apart from `m_SecondaryAoeArea` (all `pattern.Nodes`); `PatternCellData.cs:19` marks direct-hit vs scatter cells.
- **Mod status:** *mostly covered* — `AoEPreview.cs:79-92` already speaks the application-cell offset/facing (`aim.centre_here`/`aim.centre_offset`) + total tile count, so single-application patterns (circle/cone/ray/sector) are conveyed. Residual: no primary-vs-splash tile **count** breakdown, and multi-`MainCell` **scatter** patterns don't distinguish extra main-impact cells (flagged only via the shape word `aim.shape_scatter` `:127`).
- **Fix:** In `CursorTail`, replace the single total-count loop (`:89-92`) with a walk of `pattern.NodesWithExtraData` tallying `MainCell` (or `position == ApplicationNode.position`) vs the rest; when main count > 1 speak "N main-impact tiles, M splash tiles". No new API — mod already holds the `OrientedPatternData`.

#### 24. Enemy shield (starship) value + predicted shield damage
- **Sighted reads it at:** `OvertipHealthBlockVM.cs:38-44,160-183` (`UpdateEnemyShields`, gated `IsPlayerEnemy`) populates current/max shield + predicted min/max shield damage via `AbilityDataHelper.GetStarshipDamagePrediction`.
- **Mod status:** *missing* — grep `[Ss]hield` across `RTAccess/` = 0 files; the only `Starship` hits are unrelated inventory access.
- **Fix:** In the enemy HP/prediction readout (`HitPredictor` + `ProxyUnit`/`Inspect`), detect `StarshipEntity` targets and append a shield line mirroring the overtip — current/max shield + predicted min-max shield damage from `AbilityDataHelper.GetStarshipDamagePrediction(...).resultShields`, gated identically on `IsPlayerEnemy`/visibility. New localized "shield" string.

---

## (2) Parity leaks (Rule 2) — ranked by severity

> **Status: both leaks below are FIXED** (2026-07-03, compile-clean). The `HideRealHealthInUI` mask and the
> fog gate the game's own `OvertipEntityUnitVM.HideFromScreen` applies are now mirrored at both read sites.
> See the per-leak **Fixed** notes.

### High severity

#### L1. Alt+arrow "Target" buffer speaks an enemy's exact HP, defenses, and buffs with NO visibility gate
- **The leak:** `RTAccess/Buffers/UnitBuffer.cs:27-89` `Populate()` has no visibility check — `:30` name; `:32-34` reads `HitPointsLeft`/`MaxHitPoints` unconditionally; `:41` `DefenseLine()` triggers exact armor/dodge/parry rules; `:45-49` adds every `!buff.Hidden` with rank/duration. `BufferManager.cs:85-90` `TargetUnit()` = `SelectedUnit().CombatState.ManualTarget ?? TurnController.CurrentUnit` — on an enemy's turn `CurrentUnit` **is** that enemy regardless of visibility, and `ManualTarget` is never cleared when the target walks into fog (`PartUnitCommands.cs:190-201`). Reachable in one keypress.
- **What the game masks:** `OvertipEntityUnitVM.cs:98-106` `HideFromScreen` hides the entire overtip (HP + buffs) when `Unit.IsInState && IsAwake && !IsVisibleForPlayer && !IsDirectlyControllable`; `OvertipUnitHealthBlockView.cs:152` forces "???" under `HideRealHealthInUI`; `OvertipHealthBlockVM.cs:91-93` clamps HP under that flag. No `FogProbe`/`IsVisibleForPlayer`/`CurrentlySeen` appears anywhere in `Buffers/`.
- **Fix — gate to add:** In `UnitBuffer.Populate`, for a non-directly-controllable unit, when `!unit.IsVisibleForPlayer` (or `FogProbe.Classify != Visible`) emit only the name / a "not visible" line and suppress HP/AP/defenses/buffs (mirror `OvertipEntityUnitVM.HideFromScreen`). Also honor `HideRealHealthInUI` (speak "???"). In `BufferManager.TargetUnit`, treat a `ManualTarget` that is no longer visible as absent so it cannot leak a target that walked into fog.
- **✅ Fixed (2026-07-03):** `BufferManager.TargetUnit` now ignores a `ManualTarget` unless `IsPlayerFaction || IsVisibleForPlayer`, falling back to the turn's unit. `UnitBuffer.Populate` adds a fog gate right after the name: a non-party, non-visible unit emits only the name + localized `buffer.not_visible` and returns (HP/AP/defenses/buffs suppressed). The name is kept because it is already parity-safe (spoken by the whose-turn cue). The HP line additionally masks to `buffer.hit_points_hidden` under `HideRealHealthInUI`.

### Medium severity

#### L2. `ProxyUnit.Detail` exact-HP read ignores `HideRealHealthInUI` (scanner category browse + enemy review cycle)
- **The leak:** `RTAccess/Exploration/ProxyUnit.cs:99-100` speaks `HitPointsLeft + " of " + MaxHitPoints` with no guard, for any living currently-visible enemy (`IsVisible == IsVisibleForPlayer` at `:41`). Grep for `HideRealHealthInUI` across the mod = 0.
- **What the game masks:** `OvertipUnitHealthBlockView.cs:152` (`HideRealHealthInUI ? "???" : value`) and `OvertipHealthBlockVM.cs:54-64,86-93` (skips real HP, clamps under the flag). The mask is **independent of fog**, so the mod's `IsVisible`/`CurrentlySeen` gating does not cover it — concealed-health bosses/special units the game shows as "???" have their exact HP announced the instant they're cycled to.
- **Fix — gate to add:** In `ProxyUnit.Detail`, guard with `if (_unit.HasMechanicFeature(MechanicsFeatureType.HideRealHealthInUI)) → localized "???"; else → exact HP` (use the game's `MechanicEntity.HasMechanicFeature`, as `OvertipHealthBlockVM` does). Localize the masked string. Also audit the Inspect (K) tooltip path and any `CombatReads` HP exposure — `InspectExtensions.cs` honors the same feature. (This mask is also the critical guard for completeness fix #9.)
- **✅ Fixed (2026-07-03):** `ProxyUnit.Detail` now reads `_unit.HasMechanicFeature(MechanicsFeatureType.HideRealHealthInUI)` and speaks localized `scan.unit_hp_hidden` ("health unknown") instead of the exact "N of M HP" (now the localized `scan.unit_hp`). Inspect (K) and `CombatReads` were checked and need no change: Inspect renders the game's own tooltip (which already honors the mask via `InspectExtensions`), and `CombatReads` exposes cover/range/threat, not HP.

---

## Already solid — do NOT touch

These combat surfaces are confirmed fully covered and parity-safe:

- **Combat log / floating combat text corpus** — hit/miss/dodge/parry/crit, damage with type, heal, deaths, saves, kills, XP/items/PF, all via `LogTap` (universal `AddMessage` tap, minus owned/noise streams).
- **Core hit prediction** — hit%, crit, cover, dodge/parry/cover/block/evasion, damage range, per-shot bursts via `HitPredictor` (correctly fog-gated).
- **Passive enemy cover / range / threat** via `CombatReads` (`ProxyUnit` suffix).
- **Resource gauges** — AP/MP, momentum, veil, profit factor, boss/timers, round — via the turn cue, the K on-demand gauges (`HudGauges`), and the threshold/readiness one-shots.
- **Action-bar slot readout** — title, AP, range, target kind, uses, ammo, cooldown, selected/active, why-disabled, and full Space tooltip (`ProxyActionBarSlot` + tooltip scraper). *(The end-turn/ammo-cost/effective-range/veil additions above are targeted appends, not a rework.)*
- **Buffs** — gain/loss live + full buff lists via `UnitBuffer`/`Inspect`. *(DOT-tick and non-stack additions above are per-line appends.)*
- **Weapon-set swaps** via `WeaponSetAnnouncer`.
- **AoE shape / facing / legality / caught-counts / friendly-fire names** via `AoEPreview`. *(Per-unit odds and scatter split are additive.)*
- **Deployment flow** — fully covered.
- **Fog-of-war gating generally** — `Entity.IsVisibleForPlayer` folds in `!IsInFogOfWar`, so the scanner category-browse and whose-turn cue are at parity; the initiative tracker adds the acting unit unconditionally by design. The **only** un-mirrored masking rule is `HideRealHealthInUI` (leaks L1/L2 above); everything else on the parity axis is sound.

---

## Appendix: recovered verifications (2 verifiers that died mid-run)

Two Phase-3 verifiers hit the StructuredOutput retry cap and returned no verdict, so their findings were
dropped from the confirmed set above. Both were completeness candidates (no leak risk). Re-verified by hand
against the mod + decompiled source after the run:

#### R1. "Possible to push" (knockback) reticle flag — **CONFIRMED gap (low severity)**
- **Sighted reads it at:** `OvertipHitChanceBlockView.cs:65,133-140` shows the `m_Push` status icon (tooltip `UIStrings.Instance.Tooltips.PossibleToPush`) driven by `ViewModel.CanPush && IsVisible`; `OvertipHitChanceBlockVM.cs:54,156` sets `CanPush.Value = m_AbilityTargetUIData.Value.CanPush`.
- **Mod status:** *missing* — `HitPredictor` reads the very same `AbilityTargetUIData` (via `AbilityTargetUIDataCache.GetOrCreate`) but never reads `.CanPush`; grep across `RTAccess/` = 0. Exact sibling of completeness gap #2 ("possible to kill" / `CanDie`), which lives on the same struct and is also unsurfaced.
- **Fix:** In `HitPredictor.Compose`, read `ui.CanPush` and, when true, append a localized `predict.can_push` ("can knock back") clause in both terse and verbose output — same insertion point and fog gate (`IsVisibleForPlayer`) as the `CanDie` fix. Trivial; the data is already fetched.

#### R2. Predicted *remaining* AP/MP after the hovered move / ability — **CONFIRMED partial (low severity)**
- **Sighted reads it at:** the AP (`ActionPointsYellow`) and MP (`ActionPointsBlue`) pips deplete in the preview as the player hovers a move destination or an ability, showing the post-action pool at a glance.
- **Mod status:** *partial* — the mod conveys the **components** but never the explicit post-action remainder: `PathInfo.cs:69-72` speaks the move **cost** ("costs X of Y movement"), the turn cue (`CombatEvents.cs:154-178`) speaks the **current** AP/MP at turn start, and `ProxyActionBarSlot` speaks an ability's **AP cost** + current pool. The player must do the subtraction across sources. Minor accuracy nit: `PathInfo.cs:70` speaks `budget = ActionPointsBlueMax` (turn max), not current remaining, so "of Y" is stale once MP is partially spent (reachability itself is still priced from current `ActionPointsBlue` at `:54`, so it stays correct).
- **Fix (low priority):** In `PathInfo.Preview`, additionally speak `ActionPointsBlue - cost` as "N movement left after" (and switch the "of Y" reference from `…BlueMax` to current `…Blue`). Optionally append predicted-remaining-AP to `ProxyActionBarSlot`/`Targeting.ArmAnnounce` for abilities (`current AP − ability AP cost`). Not decision-blocking — the raw cost is already spoken.
